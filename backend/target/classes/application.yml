spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/petstore
    username: petstore
    password: dev_password
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080
